import React from 'react';
import ReactDOM from 'react-dom/client';
import CoursewareApp from './CoursewareApp';
import '../styles/index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CoursewareApp />
  </React.StrictMode>
);
